{
  "parents": "element must have this parent: {{placeholder}}",
  "parent": "element must have this direct parent: {{placeholder}}",
  "is": "tag must be a &lt;{{placeholder}}&gt;",
  "direct": "element must contain this direct child: {{placeholder}}",
  "contains": "element must contain this child: {{placeholder}}",
  "attrs": "element must have this attribute: {{placeholder}}",
  "siblings": "element must have this sibling: {{placeholder}}",
  "not": {
    "parents": "element must not have this parent: {{placeholder}}",
    "parent": "element must not have this direct parent: {{placeholder}}",
    "is": "tag must not be a &lt;{{placeholder}}&gt;",
    "direct": "element must not contain this direct child: {{placeholder}}",
    "contains": "element must not contain this child: {{placeholder}}",
    "attrs": "element must not have this attribute: {{placeholder}}",
    "siblings": "element must not have this sibling: {{placeholder}}"
  }
}